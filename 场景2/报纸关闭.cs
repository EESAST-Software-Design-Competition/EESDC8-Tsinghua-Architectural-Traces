using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class 报纸关闭 : MonoBehaviour
{
    public GameObject 报纸;
    // Start is called before the first frame update
    void Start()
    {
        报纸.SetActive(false);
    }

    // Update is called once per frame
    
}
